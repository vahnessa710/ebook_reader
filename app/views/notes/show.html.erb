<div class="flex h-screen bg-gray-50 dark:bg-gray-900">
  <!-- Main Content -->
  <div class="flex-1 overflow-y-auto p-6 sm:p-8">

    <div class="max-w-2xl mx-auto mt-8 p-6 bg-white dark:bg-gray-800 shadow rounded relative">   

      <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">TITLE: <%= @note.title %></h1>
      <p class="text-gray-700 dark:text-gray-300">BODY: <%= @note.body %> </p> 
       <div class="flex justify-end space-x-4 mt-auto">
        <%= link_to edit_user_note_path(current_user, @note), class: "text-red-500 hover:text-red-700 text-2xl" do %>
          <i class="fas fa-pen"></i>
        <% end %>

        <%= link_to "← Back to Notes", user_notes_path(current_user), class: "px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 text-xl" %>
      </div>
  
       
    </div>

  </div>
</div>
