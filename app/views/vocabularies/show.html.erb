<h1>Definition of "<%= @word %>"</h1>

<% if @definitions.any? %>
  <ul>
    <% @definitions.each do |definition| %>
      <li><%= definition %></li>
    <% end %>
  </ul>

 
    <% end %>
<br>
<%= link_to "Back to Search", search_form_vocabularies_path, 
  class: "inline-block px-4 py-2 bg-indigo-600 text-white rounded hover:bg-indigo-700 transition" %>

